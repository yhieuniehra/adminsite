<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chỉnh Sửa Lịch Đặt Chỗ</title>
  <!-- Link Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #fff, #fff);
      color: #fff;
    }
    .form-container {
      background: #fff;
      color: #000;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    h1 {
      font-weight: bold;
    }
    .btn-primary {
      background-color: #0c0c0c;
      border: none;
    }
    .btn-primary:hover {
      background-color: #0c0c0c;
    }
    .btn-secondary {
      color: #0c0c0c;
      border: 1px solid #0c0c0c;
    }
    .btn-secondary:hover {
      background-color: #0c0c0c;
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="container d-flex justify-content-center align-items-center vh-100">
    <div class="form-container w-50">
      <h1 class="text-center text-primary mb-4">Chỉnh Sửa admin site</h1>

      <form action="/bookings/edit/<%= booking._id %>" method="POST" class="mt-4">
        <div class="mb-4">
          <label for="customerName" class="form-label">Tên Khách Hàng:</label>
          <input 
            type="text" 
            class="form-control" 
            name="customerName" 
            id="customerName" 
            value="<%= booking.customerName %>" 
            required>
        </div>

        <div class="mb-4">
          <label for="date" class="form-label">Ngày:</label>
          <input 
            type="date" 
            class="form-control" 
            name="date" 
            id="date" 
            value="<%= booking.date.toISOString().split('T')[0] %>" 
            required>
        </div>

        <div class="mb-4">
          <label for="time" class="form-label">Giờ:</label>
          <input 
            type="time" 
            class="form-control" 
            name="time" 
            id="time" 
            value="<%= booking.time %>" 
            required>
        </div>

        <div class="d-flex justify-content-between">
          <button type="submit" class="btn btn-primary px-4">Cập Nhật</button>
          <a href="/bookings" class="btn btn-secondary px-4">Quay Lại</a>
        </div>
      </form>
    </div>
  </div>

  <!-- Link Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
